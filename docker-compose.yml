services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: avecrm
      POSTGRES_USER: ave
      POSTGRES_PASSWORD: avepwd
    volumes:
      - dbdata:/var/lib/postgresql/data
    ports: ["5432:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]

  backend-php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    working_dir: /var/www/html
    volumes:
      - ./backend:/var/www/html
    depends_on: [db, redis]

  nginx:
    image: nginx:1.27-alpine
    volumes:
      - ./backend:/var/www/html:ro          # ← Laravel code visible to nginx
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:80"
    depends_on: [backend-php]

volumes:
  dbdata:
