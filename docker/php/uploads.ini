; PHP upload limits for file uploads (CVs, images, etc.)
upload_max_filesize = 20M
post_max_size = 25M
max_execution_time = 60
memory_limit = 256M

; Opcache: revalidate on every request (safe for development with volume mounts)
opcache.revalidate_freq = 0

